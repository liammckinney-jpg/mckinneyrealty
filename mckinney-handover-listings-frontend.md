# Handover — Listings Front End, Phase 1 (staging)

**Date:** September 7, 2026 · **Branch:** `staging/listings-frontend` (from `main`; never merges until brokerage switch + PropTx credentials — Liam's explicit call) · **Spec:** `docs/specs/LISTINGS_FRONTEND_SPEC_2026-09-07.md` in the working folder (Notion copy governs)

## Shipped

- **Fixtures + provider** — `fixtures/listings.json` (40 synthetic listings, §4 shape, obviously fictional), `scripts/listings/provider.js` (ListingProvider interface; `LISTINGS_PROVIDER=fixture|proptx`; production build throws on fixture; proptx throws until Phase 1b).
- **Routes** — `/apartment-buildings-for-sale/` index + 16 market pages + 40 detail pages, generated by `npm run listings:build` from templates in `scripts/listings/templates/`. §6 strings verbatim (byte-checked). Client-side filters/sort per §5 with URL-encoded shareable state (`apartment-buildings-for-sale/listings.js` + shared card renderer).
- **Detail pages** — attribution block above the fold, reported-figures table with `Not reported` rows, remarks as supplied (line breaks preserved), JSON-LD RealEstateListing (no invented fields), request-package form on the existing consent path (`form_type: listing_package_request`, `source: listings`, MLS id in payload, GA4 `lead_submit`).
- **Underwriter prefill extension** (`tools/underwrite.html`) — new documented keys `taxes/gross/opex/noi/mls`; completes gross = noi + opex, allocates tax out of the excl.-tax opex lump; computed NOI reproduces the reported NOI; `mls` appends to `src` tracking. Existing keys untouched (Elgin link regression-tested).
- **Compliance chrome** — RECO 5.3 brokerage band in the header on every page sitewide (41 files; current live compliance line verbatim); staging banner `Sample data — not live listings. Staging only.` on all listings routes in fixture mode, non-dismissable; every listings route `noindex,follow`.
- **Build wiring** — Vercel buildCommand: `listings:build && listings:check && og:check`. `listings:check` (`scripts/listings/status-check.js`) fails the deploy on any non-Active status, stale/missing detail page, missing noindex/banner in fixture mode, or client-side provider flag. Injection test passes (Sold → exit 1).
- **Also** — `apartment-buildings-for-sale/sitemap.xml` regenerated per build (not yet referenced from robots.txt — production flip); `/listings` + `/listings/` 301 → new index (Elgin page still reachable at `/listings/58-60-elgin-street`); nav/footer "Listings" links point to the new index; CLAUDE.md §12 block appended; 410 template staged for Phase 1b.

## Decisions needed (Liam)

1. **Detail-page title/meta** — §6 has no detail-page title tag or meta description. Used `{street}, {city} — McKinney Realty` and a data-assembled meta (`{sub}. Listed by {office}.`). Bless or supply spec strings.
2. **Form confirmation microcopy** — "Request received. The package for {address} is on its way — our team will follow up directly." (modeled on Elgin's). Bless or replace.
3. **"McKinney Realty Group"** — §6 footer / §8 JSON-LD say "McKinney Realty Group", conflicting with the locked brand "McKinney Realty". Used the locked brand; the §6 MLS® footer paragraph is deferred to Phase 1b (C2 text + real data) per the spec's own staging parenthetical.
4. **One live form test** — request-package submits were tested to the validation boundary only (no fake lead email sent). Submit one yourself on the Vercel preview to confirm the Apps Script path end to end.

## Open items (Phase 1b, when credentials arrive)

- PropTxProvider against the RESO endpoint; C5 PropertyType/SubType mapping; C2 attribution/disclaimer text into the listings-route footer; photo hotlink/cache policy; og:image with attribution overlay on detail pages; robots.txt sitemap reference + noindex flip; snapshot diffing → 410 pages; set `LISTINGS_PROVIDER=proptx` in Vercel before any production build (production fails on fixture by design).
- Pre-existing fix shipped in passing: the 10 `/emails/` Brevo masters were failing `og:check` on main (deploy-blocking); exempted in `og-manifest.json` as email templates, not web pages.
